<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Calendar.IO.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Calendar.IO"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
 
    Title="Calendar.IO">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Padding="20" RowDefinitions="Auto, *" ColumnDefinitions="*">
        <!-- HEADER -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Spacing="12">
            <Button x:Name="PrevMonthBtn" Content="◄" Width="36" Height="36" />
            <TextBlock x:Name="MonthLabel"
                   FontSize="28"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center" />
            <Button x:Name="NextMonthBtn" Content="►" Width="36" Height="36" />
        </StackPanel>

        <!-- CALENDAR GRID -->
        <Grid Grid.Row="1" Margin="0,20,0,0" x:Name="CalendarGrid"
          RowSpacing="8" ColumnSpacing="8">
            <!-- Column definitions will be created in code-behind -->
        </Grid>
    </Grid>

</Window>
